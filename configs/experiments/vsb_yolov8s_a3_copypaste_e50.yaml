experiment_id: vsb_yolov8s_a3_copypaste_e50
description: Optional VSB curated benchmark experiment with experimental conservative copy-paste defects.
dataset:
  key: vsb_curated
  data_yaml: ${WOOD_DC_VSB_A3_DATASET_YAML:-/workspace/data/wood_defect_datacentric/generated_yolo/vsb/A3_copy_paste_defects/dataset.yaml}
  split_protocol: existing_curated_train_val_test
transforms:
  preprocessing: P0_baseline
  augmentation: A3_copy_paste_defects
  materialized_dataset_required: true
  experimental: true
training:
  model: yolov8s
  epochs: 50
  imgsz: 1024
  batch: 32
  workers: 4
  seed: 42
  patience: 30
  device: "0"
  optimizer: auto
  pretrained: true
  deterministic: true
  single_cls: false
outputs:
  output_root: results

